const myName = 'Jonas';

if (myName === 'Jonas') {
    // Temporal Dead Zone for job variable
    console.log(`Jonas is a ${job}`);
    const age = 2037 - 1989;
    console.log(age);
    // ------
    
    const job = 'teacher';
    console.log(x);
}